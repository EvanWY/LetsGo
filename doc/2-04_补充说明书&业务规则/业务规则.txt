业务规则：
操作规则：

         操作前：保证下载软件，并正确安装，手机的操作系统能够支持

         操作后：每添加一条计划，则会显示在列表中。

                            每删除一条计划，则会在列表中消失。

                            每次运动时开启软件，会记录运动信息。

 

结构规则：
         本软件结构简单，只有对计划列表的增删改查功能。系统大致分为操作部分和数据部分，用户通过对数据的操作来使用软件。
